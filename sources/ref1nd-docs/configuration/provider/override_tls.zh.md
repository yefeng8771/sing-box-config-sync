### 结构

```json
{
  "enabled": true,
  "disable_sni": false,
  "server_name": "example.com",
  "certificate_server_name": "example.com",
  "insecure": false,
  "kernel_tx": false,
  "kernel_rx": false
}
```

### 字段

`enabled` `disable_sni` `server_name` `certificate_server_name` `insecure` `kernel_tx` `kernel_rx` 详情参阅 [TLS 字段](/zh/configuration/shared/tls/#outbound)。
